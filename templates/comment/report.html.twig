{% extends 'base.html.twig' %}

{% block body %}
    <h1>Signaler le commentaire</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Envoyer le signalement</button>
    {{ form_end(form) }}

    <a href="{{ path('post_show', {'id': comment.post.id}) }}">Retour à l'article</a>
{% endblock %}