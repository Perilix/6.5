<nav>
    <div class="logo">
        <a href="{{ path('home') }}">
                <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 128 128" width="65" height="65">
                    <text y="1.2em" font-size="116">⚫️</text>
                    <text y="1.3em" x="0.2em" font-size="96" fill="#fff">sf</text>
                </svg>
        </a>
    </div>
    <ul>
        <li><a href="{{ path('home') }}">Accueil</a></li>
        <li><a href="{{ path('post_index') }}">Actualités</a></li>
        <li><a href="{{ path('about') }}">À propos</a></li>
        <li><a href="{{ path('contact') }}">Contact</a></li>
        {% if is_granted('IS_AUTHENTICATED_FULLY') %}
            <li><a href="{{ path('profile') }}">Mon profil</a></li>
            <li><a href="{{ path('logout') }}">Déconnexion</a></li>
        {% else %}
            <li><a href="{{ path('login') }}">Connexion</a></li>
        {% endif %}
    </ul>
</nav>